name: EmoteSniper Scheduled Run

on:
  schedule:
    - cron: '0 * * * *'  
  workflow_dispatch:    

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies (if any)
        run: npm run.js

      - name: Run EmoteSniper script
        run: node path/to/your/script.js
